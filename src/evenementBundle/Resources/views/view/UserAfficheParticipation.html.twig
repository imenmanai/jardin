{% extends 'base.html.twig' %}
{% block body %}
    <center>
        {% if(test=="current") %}
        <h2> mes participations </h2>
        {% else %}
        <h2> My Late Participations  </h2>
        {% endif %}
       {# <table border="2" >
            <td>   Event Name</td>
           <td> Event Date </td>
            <td>  Event Hour  </td>
            <td>  Event Affiche  </td>
            <td>  </td>#}
        <br>

        <br>
        <table class="table table-striped table-bordered" cellspacing="0" width="100%">
            <thead class="thead-dark" >
            <tr>
                <th scope="col">nom de l evenement</th>
                <th scope="col">l heure de debut </th>
                <th scope="col">l heure du fin d evenement</th>
                <th scope="col">affiche de l evenement</th>
                <th scope="col"></th>
                <th scope="col"></th>
            </tr>
            </thead>
            {% for f in participations %}
                <tr>
                    <td> {{ f.Event.nom }} </td>
                   <td> {{ f.Event.date | date}} </td>
                    <td> {{ f.Event.datefin | date('l , Y-m-d ') }} </td>
                    <td> <img src="{{ asset('images/' ~ f.Event.image) }} "height="50px" width="50px"/></td>
                    <td> <a href="{{ path('UserDeleteParticipation',{'id':f.id}) }}" onclick="return confirm('Do you want to delete this participation !')"> <button class="btn btn-dark"> Supprimer</button></a></td>

                    {% if(test=="late") %}

                    {% endif %}
                </tr>
            {% endfor %}
        </table>
    </center>
<br><br>


    <br>
    <br>
    <br>
    <br>
    <br>


{% endblock %}